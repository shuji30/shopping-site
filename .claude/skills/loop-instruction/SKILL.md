---
name: loop-instruction
description: >-
  ループ形式でタスクを段階的に進めるための骨子スキル。「1ループ1コミット」を規律とし、
  ROADMAP.md（これからやること）と HISTORY.md（やったこと）を更新しながら反復的に開発を進める。
  ユーザーが「ループで進めて」「1コミットずつ」「ロードマップに沿って」「反復的に実装して」
  といった段階的・継続的な作業を求めたとき、または /loop と組み合わせて自律的に作業を回すときに使う。
  目的・ゴール・禁止事項を最初に確認し、逸脱しないことを重視する。
---

# ループ指示スキル（骨子）

タスクを一気に片付けるのではなく、**小さなループの積み重ね**で前進させるための骨子。
1ループごとに「1つの意味あるまとまり」を完成させ、必ず1コミットを残す。これにより

- どこまで進んだかが履歴（HISTORY.md）から一目で分かる
- これから何をやるかがロードマップ（ROADMAP.md）で常に明確になる
- 途中で中断・再開しても、直前のコミットとロードマップから安全に再開できる

という状態を保つ。中断・再開に強く、後から経緯を追えることがこの進め方の価値。

---

## 目的

**着物レンタルのECサイトを作る。**

- **形態**: レスポンシブWeb＋アプリ両対応。まずWebで作り、将来 Capacitor でiOS/Androidアプリ化できる構成にする（1コードベースで両対応）。
- **最初のMVP**: 商品カタログの表示のみ（一覧・詳細）。カート・予約・在庫カレンダー・決済・認証は後続フェーズに回す。
- **技術スタック**: Next.js (App Router) + TypeScript + Tailwind CSS。将来のアプリ化に Capacitor（既存 Monaca/Cordova の後継）。MVP のデータは JSON/静的データで持つ。

以降のすべてのループは、この目的に照らして判断する。目的を変える必要が出たらユーザーに確認する。

---

## ゴール

ループを回した結果として到達したい状態を、検証可能な形で定義する。
「なんとなく良くなった」ではなく、達成/未達成が判定できる条件にすること。

### MVP のゴール（カタログ表示）

- [ ] レスポンシブWebで着物商品の**一覧**が閲覧できる（カテゴリ等で絞り込める）
- [ ] 各商品の**詳細**（画像・レンタル料・サイズ・素材/説明）が見られる
- [ ] スマホ / タブレット / PC のいずれでもレイアウトが崩れず表示される
- [ ] 将来アプリ化できる構成（Next.js + Capacitor 想定）になっている

### 以降のフェーズ（MVP 外・ロードマップに記載）

カート／レンタル期間選択／在庫カレンダー／ユーザー認証／オンライン決済／
予約・配送・返却フロー／Capacitor でのアプリ化 —— は MVP 達成後に着手する。

各ゴールは ROADMAP.md のタスク群に分解し、ループを通じて1つずつ潰していく。
MVP のゴールにすべてチェックが付いたら、次フェーズへ進むか一区切りとする（→「終了条件」）。

---

## 禁止事項

ループの規律を壊す行為は禁止する。理由も添えるので、状況が違えば判断の助けにすること。

- **1ループで複数の無関係な変更をまとめてコミットしない**
  — 履歴が追えなくなり、問題発生時に切り戻す単位が失われるため。
- **ロードマップ・履歴を更新せずに次のループへ進まない**
  — 進捗の唯一の記録源なので、更新漏れは「どこまでやったか」を失うことに直結する。
- **目的・ゴールから外れた作業を勝手に追加しない**
  — スコープが膨張し、いつまでも終わらなくなる。必要ならまずロードマップに追記して合意を取る。
- **壊れた状態（ビルド不能・テスト赤）のままコミットで1ループを閉じない**
  — 各コミットは「安全に再開できる地点」であるべき。直せないなら履歴に理由を明記して止める。
- **禁止事項・ゴール・目的を無断で書き換えない**
  — これらは合意の土台。変更が要るときはユーザーに確認してから。

---

## 1ループの定義

1ループ = 「レビュー可能で、単独で意味を持つ最小のまとまり」。
大きすぎると1コミットに複数の関心事が混ざり、小さすぎると履歴がノイズになる。
迷ったら「このコミットのメッセージを1行で書けるか？」を基準にする。書けないなら分割する。

---

## ループの実行手順

各ループで以下を順に行う。

1. **確認**: 「目的」「ゴール」「禁止事項」を読み直し、今回のループの狙いを1つに絞る。
2. **選択**: ROADMAP.md から次に着手するタスクを1つ選ぶ（原則、上から順／優先度順）。
3. **実行**: そのタスクだけを実装・変更する。スコープ外に手を出さない。
4. **検証**: ビルド・テスト・動作確認など、可能な範囲で「壊れていない」ことを確かめる。
5. **記録**:
   - HISTORY.md に「今回やったこと・結果・気づき」を1エントリ追記する。
   - ROADMAP.md のタスク状態を更新する（完了/進行中/新規発見タスクの追加）。
6. **コミット**: 変更（コード + ROADMAP + HISTORY）をまとめて**1コミット**する。
7. **判定**: 「終了条件」を満たしたか確認。満たさなければ次のループへ。

---

## コミット規約

- 1ループ = 1コミット。コード変更と ROADMAP/HISTORY の更新を同じコミットに含める
  （進捗記録と成果物が常に一致するようにするため）。
- メッセージは「何を・なぜ」が分かる1行を先頭に置く。必要なら本文で補足。
- 慣例として接頭辞を付けると履歴が読みやすい（例: `feat:` `fix:` `docs:` `chore:` `refactor:`）。

**例:**
```
feat: ログイン画面のバリデーションを追加

- 空入力時にエラーメッセージを表示
- ROADMAP: タスク3 を完了、関連してタスク7（i18n対応）を新規追加
```

---

## ロードマップと履歴

このスキルと同じディレクトリに2つの生きたドキュメントを置く。
テンプレートであると同時に、このスキル自身の開発を追跡する実ファイルでもある。

- **ROADMAP.md** — これからやること。タスク一覧と状態（未着手/進行中/完了）。
  各ループの冒頭で読み、末尾で更新する。
- **HISTORY.md** — やったこと。ループごとの追記式ログ。新しいものを上に積む。

実際のプロジェクトで使うときは、このファイルを作業対象の場所へコピーするか、
リポジトリ直下に同名ファイルを用意して運用してよい。

---

## 終了条件

以下のいずれかを満たしたらループを止める。

- 「ゴール」のチェックがすべて埋まった。
- ROADMAP.md の残タスクがゼロになった。
- ユーザーから停止指示があった。
- これ以上進めるには目的・ゴールの見直しが必要になった（→ ユーザーに確認）。

止めるときは、HISTORY.md に「なぜ止めたか／残課題」を最後のエントリとして残す。
