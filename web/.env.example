# このファイルをコピーして .env を作成してください: cp .env.example .env
# SQLite（開発用）。スキーマファイル(prisma/schema.prisma)からの相対パス。
DATABASE_URL="file:./dev.db"

# 管理画面(/admin)の Basic 認証。ADMIN_PASSWORD が空だと管理画面は全拒否される。
ADMIN_USER="admin"
ADMIN_PASSWORD="change-me"
